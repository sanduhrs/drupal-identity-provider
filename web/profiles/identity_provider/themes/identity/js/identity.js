/**
 * @file
 * Identity behaviors.
 */

(function ($, Drupal) {

  'use strict';

  /**
   * Behavior description.
   */
  Drupal.behaviors.identity = {
    attach: function (context, settings) {

    }
  };

} (jQuery, Drupal));
